<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blue_purple</item>
        <item name="colorPrimaryDark">@color/purple_blue</item>
        <item name="colorAccent">@color/pink_pink</item>
        <item name="android:textColor">@color/light_black</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="text_theme" parent="AppTheme">
        <item name="android:textSize">28sp</item>
    </style>

    <style name="AppThemeToolbar" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blue_purple</item>
        <item name="colorPrimaryDark">@color/purple_blue</item>
        <item name="colorAccent">@color/pink_pink</item>
        <item name="android:textColor">@color/light_black</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ToolbarTheme">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
    </style>
</resources>
